{"phase":"00","status":"none-yet","note":"Record failures here as JSON lines with symptom, cause, and fix."}
{"phase":"tooling","symptom":"`npm run format` failed because Biome treated the quoted `**/*.mjs` glob as a literal missing path.","likely_cause":"Biome path handling did not expand the shell glob in the npm script as intended.","fix":"Format the JavaScript source directories directly with `biome format --write bin src test`."}
{"phase":"04","symptom":"Initial Phase 04 test run failed to parse `test/app.test.mjs` because `await` was used inside a non-async callback passed to `assert.rejects`.","likely_cause":"The test setup created a temporary cwd inline inside the callback instead of before the assertion.","fix":"Move `mkdtemp` to the async test body before calling `assert.rejects`."}
{"phase":"06","symptom":"Initial context-packing test expected a workspace context section even when the temporary run workspace had no files.","likely_cause":"The assertion coupled base system-prompt behavior to non-empty file context.","fix":"Assert the base Kodr system prompt in empty workspaces and cover AGENTS.md/file context in dedicated tests."}
{"phase":"10","symptom":"Initial proposal-flow test run failed because an existing artifact list assertion did not include `writes.json` and `tests.json`.","likely_cause":"The Phase 10 artifact layout expanded but the Phase 04 assertion was still exact-match old layout.","fix":"Update the expected artifact map to include proposal and verification artifacts."}
{"phase":"17","symptom":"The first skill-cap test could not load a skill by frontmatter name after truncating the file inside frontmatter.","likely_cause":"Byte caps can intentionally prevent frontmatter from being fully parsed, so the requested name was unavailable.","fix":"Keep path-based skill loading as the deterministic fallback and test truncated skills by path."}
{"phase":"18","symptom":"The first generated todo app failed because it tried to mkdir the todo JSON file path and had list() log instead of return data.","likely_cause":"The prompt requested behavior but did not constrain the implementation contract tightly enough for store methods and tests.","fix":"Repair the example manually, keep the failing artifact, and document the prompt lesson."}
{"phase":"18","symptom":"The second generated todo app test failed with require is not defined inside an ESM test file.","likely_cause":"The prompt said ESM but did not explicitly forbid CommonJS globals, and the model mixed module styles in tests.","fix":"Update the prompt to forbid require/module/__dirname and keep root verification reporting failed runs as ok=false."}
{"phase":"21","symptom":"The first Markdown blog example dry run failed during POST /chat/completions with fetch failed after several minutes.","likely_cause":"Local model transport or LM Studio runtime interruption during a larger generation request.","fix":"Add failure artifacts for model run failures before retrying the example so failed harness trials leave prompt, context, summary, and error evidence."}
{"phase":"21","symptom":"The retry with apply and subproject tests also failed during POST /chat/completions with fetch failed, leaving failure artifacts under .kodr/runs/2026-05-26T09-23-40.825Z.","likely_cause":"The local model connection dropped while generating the larger Markdown blog app response.","fix":"Use the new failure artifacts for diagnosis and complete the second example manually, keeping the generated-example prompt for future retries."}
{"phase":"22","symptom":"The first permission-policy integration test changed the default read escape error from the safe-write jail to a policy error.","likely_cause":"The default policy duplicated relative path validation before the existing jail instead of only narrowing configured restrictions.","fix":"Make unrestricted default path policy defer to downstream jail validation while restrictive path policies still deny early."}
{"phase":"23","symptom":"The notes API generation produced only package.json, and npm test passed because node --test found zero tests.","likely_cause":"Verification treated a zero-test Node test run as success when the example prompt required native tests.","fix":"Mark test commands that report tests 0 as failed, then complete the API fixture manually."}
{"phase":"23","symptom":"The first zero-test detection rule marked a real generated subproject test run as failed when nested node --test output omitted summary counts.","likely_cause":"The heuristic relied on stdout only, but nested test invocations can print the npm script command without the usual Node test summary lines.","fix":"When stdout lacks counts, fall back to detecting actual test files in the verification cwd."}
{"phase":"24","symptom":"The initial MCP duplicate-provider test used assert.rejects for a synchronous constructor error.","likely_cause":"Provider validation happens during client construction before any promise is returned.","fix":"Use assert.throws for synchronous validation and keep async rejects for listTools failures."}
{"phase":"25","symptom":"The CSV expense example generation failed during POST /chat/completions with fetch failed after several minutes.","likely_cause":"Local LM Studio transport dropped during a larger generation request.","fix":"Use the recorded failure artifacts and complete the example manually, since model-run failure artifacts are already implemented."}
{"phase":"26","symptom":"A smaller CSV parser slice still failed during POST /chat/completions with fetch failed.","likely_cause":"The local model response path was still vulnerable to a long-running non-streaming request dropping before completion.","fix":"Add --stream support for run completions and retry the slice with streaming enabled."}
{"phase":"26","symptom":"The streaming parser slice applied successfully but verification failed because tests expected the old missing-column diagnostic.","likely_cause":"The model made a valid parser diagnostic improvement without updating the matching test expectation.","fix":"Run a smaller Kodr test-repair slice and record the artifact."}
{"phase":"26","symptom":"CSV test repair slices repeatedly regressed the escaped-quote fixture while fixing syntax or diagnostics.","likely_cause":"The harness only accepts full-file proposals, so a tiny assertion repair gave the model too much surface area to rewrite unrelated nearby content.","fix":"Stabilize the fixture, record the provenance honestly, and add a future patch-oriented repair phase."}
{"phase":"27","symptom":"The first CSV regeneration core slice rewrote the whole core file and failed verification because parseCsv was not exported, parseCsv returned a flat field list, rowsToExpenses returned an analysis object, and returned expense objects referenced undefined variables.","likely_cause":"A full-file regeneration asked the model to hold too many related invariants without a patch-sized repair loop.","fix":"Move patch-oriented repairs before continuing CSV regeneration and record the failed run artifacts."}
{"phase":"27","symptom":"The first patch repair proposal failed to apply because the model double-escaped newlines in the patch search text.","likely_cause":"The JSON response represented patch text with literal backslash-n sequences instead of actual newlines.","fix":"Normalize common patch escapes only when the original search matches zero times and the normalized search matches exactly once."}
{"phase":"27","symptom":"The second patch repair applied successfully but verification failed because the model duplicated helper functions instead of addressing the full failing task list.","likely_cause":"Patch application solved the edit-surface problem, but the model still lacked a task/scratchpad mechanism for tracking all required repairs.","fix":"Keep patch-oriented repairs and leave memory/scratchpad as the next planning pressure before completing CSV regeneration."}
{"phase":"28","symptom":"A combined CSV source-and-test patch proposal failed validation because the second patch lacked a replace field and targeted hallucinated surrounding text.","likely_cause":"The prompt still bundled two edit tasks into one proposal and the model lost schema discipline on the second patch.","fix":"Record invalid proposal artifacts and split the source and test changes into separate patch prompts."}
{"phase":"28","symptom":"A source-only patch failed because the model changed harmless horizontal whitespace in the search anchor, producing zero exact matches.","likely_cause":"Exact patch matching is safe but brittle when model output drifts spaces around syntax.","fix":"Add whitespace-tolerant matching for unique same-line-window patches, then rerun the exact source patch successfully."}
{"phase":"29","symptom":"The CSV regeneration retries had to encode repair intent repeatedly in prompt files and provenance notes.","likely_cause":"Kodr had run artifacts and task plans but no explicit durable memory scope or run-local scratchpad field for carrying concise repair state.","fix":"Add committed project memory, private local user memory, and scratchpad artifacts on run proposals."}
{"phase":"30","symptom":"Process rules such as example provenance, patch-first repair, and review-cycle expectations were learned in chat before being promoted into AGENTS.md or project memory.","likely_cause":"Kodr had no delegated review step that compared transcript direction against committed repo instructions.","fix":"Add a cycle-review subagent that reports likely missing AGENTS.md guidance without editing it automatically."}
{"phase":"31","symptom":"A scaffold repair run reported no writes, but earlier patches in the same failed batch had already changed files.","likely_cause":"Patch preparation validated and applied patches one at a time, so a later stale patch could fail after earlier target files were already written.","fix":"Validate the whole patch batch and compose target contents before performing any filesystem writes."}
{"phase":"31","symptom":"An exact test repair with several patches to one file left only the last same-file change in place.","likely_cause":"Each patch was computed from original file content and then each result was written sequentially to the same path, overwriting previous patches in the batch.","fix":"Group patches by target and apply them in memory in proposal order before writing each target once."}
{"phase":"31","symptom":"The provenance-only repair timed out before returning model output.","likely_cause":"Local LM Studio calls can run for several minutes and still fail to produce a usable completion.","fix":"Record the timeout artifact and keep loop budgets as the next phase to make long local runs easier to bound and inspect."}
{"phase":"31","symptom":"The final Markdown search repair proposal was rejected because it used placeholder anchors and stale indentation.","likely_cause":"The local model reasoned through the correct intent but did not preserve exact current source text in its patch search strings.","fix":"Safe-writes rejected the batch without partial application; the proposal was inspected and the narrow intent was stabilized manually with provenance."}
{"phase":"32","symptom":"Prompt fixtures repeated JSON schema boilerplate such as files, patches, and scratchpad shapes.","likely_cause":"The proposal protocol lived partly in each user prompt instead of being centralized in the harness system prompt.","fix":"Move the response envelope into the system prompt and clean active Markdown-search prompts so they focus on task requirements."}
{"phase":"post-32","symptom":"A full-repo Pong generation against meta/llama-3.3-70b failed because LM Studio loaded the model with a 4096 token context.","likely_cause":"Kodr packed normal repo context, but the local model context setting did not persist and was much smaller than the model supports.","fix":"Do not optimize Kodr for 4k context on this model; prefer a stable 16k-32k LM Studio context and add future scoped-context controls for example work."}
{"phase":"post-32","symptom":"A reduced-context Pong run returned a fenced envelope, but Kodr reported proposalFound false and still ran verification.","likely_cause":"The JSON extractor rejected invalid model escapes like backslash-single-quote, and run verification was not gated on a successfully extracted proposal.","fix":"Repair invalid non-JSON string escapes during extraction and mark apply/test runs failed when no proposal is available instead of running verification."}
{"phase":"post-32","symptom":"A 70B repair run pushed the machine into high memory pressure and swap while still processing the prompt.","likely_cause":"Large local model context and high offload settings can exceed practical memory headroom even when the model supports the requested context length.","fix":"Stop the generation, avoid further 70B calls during high memory pressure, and keep the failed Pong output as a learning artifact rather than repairing it into a canonical example."}
{"phase":"34","model":"openai/gpt-5.4-nano","symptom":"Generated test used promisify(execFile) without try/catch when the CLI exits non-zero, causing the test to throw instead of assert","likely_cause":"Model knew execFile rejects on non-zero exit but still wrote a bare await that would always throw, contradicting its own comment","fix":"Wrap in try/catch and read error.stdout and error.code for assertions"}
{"phase":"37","symptom":"node --test grandchild exits 0 and skips files when parent is also node --test","cause":"Node.js 24 added a recursive-call guard in the test runner: detecting NODE_TEST_CONTEXT or NODE_CHANNEL_FD in the environment causes `run()` to skip file discovery entirely","fix":"Strip NODE_TEST_CONTEXT and NODE_CHANNEL_FD from spawnCommand env before launching verification subprocesses"}
{"phase":"37","symptom":"eval against koder-by-codex workspace returns empty files or malformed proposals","cause":"Model sees existing todo-cli source in context pack and returns either empty files (already present) or messages with wrong field names (level/content vs type/text)","fix":"For eval use-cases with code generation prompts, use a workspace cwd that does not already contain the target files, or pass a minimal system prompt"}
{"phase":"39","symptom":"fetch_url SSRF guard validated only the initial host; a redirect to a private/metadata address would be followed","likely_cause":"Node fetch defaults to redirect:'follow', so host validation never applied to redirect targets","fix":"Set redirect:'manual' and throw on any 3xx response so no redirect is ever followed"}
{"phase":"39","symptom":"kodr run --tools --stream silently produced no tool calls and a forced stop finish reason","likely_cause":"The SSE reader only accumulated delta.content and ignored delta.tool_calls fragments","fix":"Merge tool_calls fragments by index, append streamed function.arguments, and surface tool_calls with a tool_calls finish reason"}
{"phase":"39","symptom":"runs failed against OpenAI-compatible servers that do not implement GET /models even with an explicit --model","likely_cause":"runPrompt always called listModels and discarded the result when options.model was set","fix":"Only call listModels when no model is provided; keep discovery in probe"}
{"phase":"39","symptom":"Live kodr run for an Express notes example, executed inside the kodr repo, returned status OK with files:[] and an info message saying it would 'read roadmap.md to identify the first unchecked phase'","likely_cause":"The context pack includes the repo's AGENTS.md Required Loop, so the model adopted the kodr-maintainer persona and tried to follow the dev workflow instead of generating the requested example app","fix":"Run generation prompts from a workspace cwd that does not contain the kodr AGENTS.md/roadmap (a clean temp dir), or strip self-referential process docs from the context pack for generation runs; harness behavior (streaming, usage capture, proposal extraction, dry-run) was correct throughout"}
{"phase":"46","symptom":"A scripted `kodr tui` local LM Studio turn completed, then exited with `readline was closed` when piped stdin ended.","likely_cause":"The readline loop treated EOF after a processed turn as an unhandled exception instead of a normal terminal-channel close.","fix":"Catch the readline-closed condition in `runTui`, return an EOF result, and add a regression test for piped input ending after a turn."}
{"phase":"47","symptom":"A live `kodr tui --stream` LM Studio test printed the final summary on the same terminal line as the last streamed token.","likely_cause":"The TUI wrote streamed chunks directly to stdout but did not insert a separator before rendering the post-run summary.","fix":"Write a newline when a streamed turn finishes before rendering the final TUI summary, with a regression assertion."}
{"phase":"52","symptom":"Inspection-aware context selected `buildWorkspaceContext` as a one-line chunk during a repo smoke test.","likely_cause":"The JavaScript scanner treated indented variables inside a function body as symbols, so the next inner variable ended the outer function span immediately.","fix":"Ignore indented lines in the JS/TS symbol scanner so the first-pass structural index tracks top-level symbols for chunk boundaries."}
{"phase":"52-example","symptom":"Express migration repair prompts included a generated package-lock.json in default context, inflating prompt size with dependency tree data.","likely_cause":"Context packing listed and sourced package lockfiles like ordinary text files instead of treating them as map-only dependency artifacts.","fix":"Keep lockfiles in file inventory and tools-mode file maps, but omit their contents from default packed context unless explicitly read."}
{"phase":"52-example","symptom":"Two Kodr repair attempts for the Express example failed after several minutes with `POST http://localhost:1234/v1/chat/completions failed: fetch failed`.","likely_cause":"Local LM Studio transport/model instability during long repair calls; a probe between failures succeeded, and inspection context was confirmed to be small.","fix":"Retry with a shorter symbol-targeted prompt; keep transport artifacts in the example run history for later hardening."}
{"phase":"52-example","symptom":"Kodr could edit package.json to add Express, but the driver had to run npm install before tests could execute against the dependency.","likely_cause":"run_command is intentionally verification-only and does not allow package installation workflows.","fix":"Add Phase 57 to design a controlled dependency install workflow with install artifacts and command policy."}
{"phase":"54","symptom":"Model rewrote src/code-inspector.mjs from scratch instead of making a targeted edit","cause":"File content was not in context — only structural summaries were packed. Model hallucinated a completely different implementation.","fix":"Use --inspect-context so the actual function body is included in the prompt. Strengthened system prompt to prefer patches over full-file rewrites for existing files."}
{"phase":"54","symptom":"--yes applied a hallucinated full-file rewrite of src/code-inspector.mjs before tests ran, clobbering the real file","cause":"The model used files[] (full rewrite) even with inspect-context. The --yes flag applies writes before the test step, so there is no safety net against hallucinated content replacing good files.","fix":"For self-dev runs, use dry-run (no --yes) and review the proposal before applying. Consider adding a --no-overwrite flag or requiring patches-only mode for existing files. Restored from git checkout."}
{"phase":"54","symptom":"Model patch search string used assert.equal instead of assert.deepEqual and wrong indentation for test/code-inspector.test.mjs","cause":"inspect-context gives the test function region but not the exact text inside the it() block; the model inferred the wrong assertion variant and indentation depth","fix":"Provide the exact surrounding lines in the prompt when a precise patch match is needed. For inner test body content, consider including more context lines in the inspect-context selection."}
{"phase":"55","symptom":"First run applied patches before test command was allowlist-checked, leaving app.mjs partially modified","cause":"--yes applies writes then runs tests; if the test command itself is invalid the writes are already committed to disk","fix":"Reverted with git checkout and reran on clean state. Long-term: consider a dry-run-first mode that validates test command before applying writes."}
{"phase":"56","symptom":"Model added --languages to assignValue handler but missed adding it to the flag dispatch list that marks it as a value-consuming argument","cause":"The arg dispatch list and the assignValue switch are two separate code locations; the model patched one but not the other","fix":"Added --languages to the dispatch list manually; recorded as a pattern to include both locations in future multi-file prompts"}
{"phase":"57-example","example":"note-linker","symptom":"Files written to kodr root instead of example directory","cause":"kodr run was invoked from the project root, so files[] paths are relative to root. Model wrote note-linker.mjs not examples/phase-57/note-linker/note-linker.mjs","fix":"Run kodr from the example directory, or prefix file paths in the prompt with the example subdirectory"}
{"phase":"57-example","example":"note-linker","symptom":"fixtureDir() in tests returned a path string without creating the directory — mkdtemp was imported but unused","cause":"Model used Date.now()+random path construction instead of calling mkdtemp. Directory did not exist so all tests failed with ENOENT","fix":"Made fixtureDir() async, replaced body with mkdtemp call. Logic was correct. Hand-edited per process.","severity":"minor"}
{"phase":"58-example","example":"postgres-docs-api","symptom":"Initial Kodr generation produced README content twice","cause":"The model returned both a full-file README update and a README patch; applying both duplicated sections.","fix":"Do not hand-fix the example. Record as a proposal-composition issue for future apply review/noise detection."}
{"phase":"58-example","example":"postgres-docs-api","symptom":"docker compose up -d failed before Postgres could start","cause":"Docker daemon was not available at unix:///Users/paul/.docker/run/docker.sock in this session.","fix":"Record environment blocker. Future dependency/service workflow should detect Docker availability before recommending service tests."}
{"phase":"58-example","example":"postgres-docs-api","symptom":"Generated tests crashed with RangeError: Maximum call stack size exceeded and Node out-of-memory failures","cause":"Generated test helper named fetch recursively called itself instead of globalThis.fetch, so HTTP tests never reached the API/database behavior cleanly.","fix":"Do not hand-fix the example. Attempted Kodr repair runs and recorded artifacts."}
{"phase":"58-example","example":"postgres-docs-api","symptom":"Kodr repair runs returned OK with zero file changes and scratchpad-only plans, or exhausted tool-call turn budget","cause":"The harness treats scratchpad-only OK envelopes as successful runs and tool-mode repair can spend its budget inspecting without producing a patch.","fix":"Updated Phase 71 plan to treat repeated scratchpad-only repair turns as no-progress and to forward scratchpad/failure context through self-healing loops."}
{"phase":"82","symptom":"Manual CLI test showed no progress messages and the planner OpenRouter override did not appear to be called","cause":"Progress events were only rendered by the TUI, and the saved run used the normal agent path rather than --subagent-stages where --agent-model overrides apply.","fix":"Render non-JSON CLI progress events to stderr and warn when --agent-model is supplied without --subagent-stages."}
{"phase":"82","example":"phase-82d","symptom":"Subagent implementer returned valid JSON syntax but invalid Kodr proposal shape: files was an array of strings and patches used content fields","cause":"Kodr relied on prompt-only JSON instructions for proposal/review turns and did not send structured-output response_format schemas.","fix":"Add proposal and review JSON schemas to chat completion requests and record them in request artifacts."}
{"phase":"82","example":"phase-82d","symptom":"Nemotron local implementer could spend tens of thousands of reasoning tokens and exceed long request timeouts","cause":"Kodr had no request-level way to pass a reasoning/thinking-token cap for models that support it.","fix":"Add opt-in --max-thinking-tokens, passed as max_thinking_tokens in chat completion requests."}
{"phase":"82","example":"phase-82e","symptom":"Planner returned proposal-shaped JSON instead of a Markdown plan, while Nemotron implementer/reviewer returned empty content with JSON in reasoning_content","cause":"The top-level proposal response_format leaked into the planner stage, and Kodr only read message.content even for structured-output responses from a reasoning model.","fix":"Disable response_format for planner stages and fall back to JSON-looking reasoning_content when message.content is empty."}
{"phase":"58-example","example":"postgres-docs-api","model":"nvidia/nemotron-3-nano-omni","symptom":"Nemotron repair created root-level utils.js instead of patching tests/utils.js","cause":"The model identified the recursive fetch helper pattern but wrote the corrected helper to the wrong path, leaving users/documents tests importing the broken helper.","fix":"Do not hand-fix. Record as a path-targeting repair failure; future repair loops should validate that changed paths overlap the failing stack trace and requested files."}
{"phase":"58-example","example":"postgres-docs-api","symptom":"docker compose initially hung on docker-credential-desktop get while pulling public postgres image","cause":"Docker Desktop credential helper blocked the compose pull path.","fix":"Retried with a temporary DOCKER_CONFIG containing an empty config.json; pull and compose startup succeeded."}
{"phase":"58-example","example":"postgres-docs-api","symptom":"Generated migration failed unless DATABASE_URL was explicitly provided","cause":"The generated app emitted .env.example but did not load .env or document that the driver must export DATABASE_URL for npm run migrate.","fix":"Do not hand-fix. Record as service setup gap for dependency/install/service workflow phases."}
{"phase":"58-example","example":"postgres-docs-api","model":"nvidia/nemotron-3-nano-omni","symptom":"Fresh take2 run failed with only ModelClientError: fetch failed in Kodr artifacts","cause":"LM Studio server.log showed the request was small, generation produced a very large reasoning trace with empty assistant content, and the client disconnected after several minutes; the harness discarded the transport cause and timing details.","fix":"Record structured transport details in error.json/summary.json and preserve raw-request size so future failures distinguish timeout, disconnect, HTTP, invalid JSON, and context-size problems."}
{"phase":"58-example","example":"postgres-docs-api","symptom":"Take2 context listed files from its own .kodr-nemotron-test2 run directory","cause":"The context packer ignored .kodr exactly but not named run output directories such as .kodr-nemotron-test2 or .kodr-repair-1.","fix":"Ignore .kodr and .kodr-* directories during context file discovery."}
{"phase":"58-example","example":"postgres-docs-api","model":"nvidia/nemotron-3-nano-omni","symptom":"Take2 retry still failed at about 300 seconds even though --timeout-ms was 600000","cause":"The richer error artifacts exposed UND_ERR_HEADERS_TIMEOUT from Node's fetch/undici transport. LM Studio generated tokens for minutes without sending response headers, so undici's default 300s headers timeout fired before Kodr's AbortSignal timeout.","fix":"Replace fetch-based model transport with zero-dependency node:http/node:https requests so Kodr's configured timeout governs slow local model calls."}
{"phase":"58","example":"postgres-docs-api","model":"nvidia/nemotron-3-nano-omni","symptom":"After transport was fixed, the model completed in one giant 15-file response with incomplete tests and stale README instructions","cause":"The harness task plan was an artifact generated after the response, not a control loop that forced planning, bounded implementation, verification, or repair before success.","fix":"Introduce staged complex execution: plan-only turn, capped implementation turns, refreshed context between stages, and incomplete-run failure when STAGED_DONE or verification is not reached."}
{"phase":"58","example":"postgres-docs-api","model":"nvidia/nemotron-3-nano-omni","symptom":"First staged retry produced a good plan, then a no-op implementation turn with files: [] while claiming Stage 1 complete","cause":"The staged controller treated any zero-path non-STAGED_DONE response as terminal instead of feeding back that no files changed. The human summary also called a failed applied run dry-run because applied=false was overloaded.","fix":"Retry no-op staged turns with explicit corrective feedback, derive staged budget from --max-turns with a cap, and report apply-requested failed runs as not applied instead of dry-run."}
{"phase":"58","example":"postgres-docs-api","model":"nvidia/nemotron-3-nano-omni","symptom":"Take4 reached STAGED_DONE and applied 17 writes, but Kodr reported ok=true even though tested=false and the generated tests failed under node --test because describe was not defined.","cause":"Staged completion trusted the model's STAGED_DONE message as success when no verification command was provided.","fix":"Mark applied staged runs that reach STAGED_DONE without verification as failed with StagedUnverifiedError so generated examples cannot pass on model self-report alone."}
{"phase":"65","example":"postgres-docs-api","model":"nvidia/nemotron-3-nano-omni","symptom":"Take5 correctly failed as unverified, but the next useful command still required a human to install generated Node dependencies before npm test could expose generated test failures.","cause":"Kodr had verification and command allowlists but no separate dependency-install workflow for package.json changes.","fix":"Add explicit --install support with npm install/npm ci allowlisting, install.json artifacts, and install-before-test ordering."}
{"phase":"65","example":"postgres-docs-api","model":"nvidia/nemotron-3-nano-omni","symptom":"Take6 installed dependencies successfully and failed npm test on ReferenceError: describe is not defined, then a small Kodr repair prompt stayed alive beyond the configured 600000ms timeout and left only context.md, prompt.md, and raw-request.json artifacts.","cause":"Phase 65 correctly reaches verification failure, but Kodr does not yet have a bounded verification-repair loop or whole-turn timeout artifacting for hung repair calls.","fix":"Fold this into Phase 72: feed tests.json plus failing paths into narrow repair context, enforce whole-turn wall-clock timeout, and write failure artifacts even when the model transport never returns."}
{"phase":"72","symptom":"A repair proposal can create or modify a sibling file while leaving the stack-trace path broken, which makes the workspace look changed without addressing the failure.","cause":"Earlier repair flow only checked whether any write occurred, not whether the write touched the requested failing path.","fix":"Require repair writes to touch at least one failure path when the failing path can be inferred from test output; stop with wrong_path otherwise."}
{"phase":"76","example":"todo-cli-docker-kodr","model":"nvidia/nemotron-3-nano-omni","symptom":"A Docker-sandboxed run reported ok=true with zero writes after the model emitted duplicate top-level files keys; the later empty files array overwrote the earlier generated files and old tests passed.","cause":"JSON.parse silently keeps the last duplicate object key, so malformed model output can hide generated writes and turn verification into a false positive.","fix":"Reject duplicate top-level JSON keys during extraction before parsing model proposals."}
{"phase":"76","example":"todo-cli-docker-kodr","model":"nvidia/nemotron-3-nano-omni","symptom":"Tool-mode attempt produced invalid patches; no-tools packed context produced a concrete three-file change that Docker correctly rejected for a syntax error, then a Kodr repair run fixed src/store.mjs and Docker tests passed.","cause":"For this small example, direct packed context was easier for the local model than multi-turn tool use. Docker sandboxing provided a clean verification boundary and kept containers for inspection.","fix":"Keep the generated example artifacts under examples/phase-76/todo-cli-docker-kodr and use the result to guide future prompt/context tuning."}
{"phase":"78","symptom":"Phase 78 example run reported npm test passed after the model produced zero files","cause":"npm test in an example subdirectory without package.json climbed to the repository root package and ran the Kodr repo tests","fix":"Require package.json in the verification cwd before running npm verification commands so missing generated projects fail instead of passing parent tests."}
{"phase":"78","symptom":"Phase 78 hook demo example did not converge after multiple Nemotron repair turns","cause":"The initial Stop-hook retry produced a valid app with shallow tests; deeper CLI checks exposed generated bugs, and subsequent local-model repair turns introduced invalid ESM and corrupt partial patches.","fix":"Do not hand-fix the example. Preserve the run artifacts as learning evidence and use the verification false-positive fix so future missing package.json examples fail correctly."}
{"phase":"83","example":"phase-82f","symptom":"A correct three-file OpenRouter subagent run used 35,005 tokens and failed review because npm test was requested without package.json.","cause":"Reviewer context duplicated the full plan and proposal, the reviewer reread files and ran verification itself, and subagent orchestration bypassed Kodr's deterministic install/test pipeline.","fix":"Use compact handoffs, run install and verification before review, and resolve npm test to node --test when native tests exist without package.json."}
{"phase":"60","symptom":"The local OpenShell integration probe failed because openshell 0.0.20 does not expose the documented sandbox exec command.","cause":"OpenShell is alpha software and installed CLI command surfaces can differ from current documentation even when upload and delete commands exist.","fix":"Capability-probe every required sandbox command before model calls, fail with setup guidance, and record openshell.json without silently falling back to host or Docker execution."}
{"phase":"60-review","symptom":"The first OpenShell executor uploaded top-level directories, ignored requested command working directories, retained stale uploaded files, and did not exclude several common credential files.","cause":"Fake-runner tests validated command presence but not the external CLI's upload destination semantics or the full workspace synchronization boundary.","fix":"Upload files to exact sandbox paths, map workspace-relative command cwd values, remove stale tracked paths, expand credential exclusions, add lifecycle tests, and keep Phase 60 open until a compatible real integration run passes."}
{"phase":"60","symptom":"The first Kodr run against the upgraded OpenShell 0.0.56 gateway failed during sandbox creation with an unexpected --no-bootstrap argument.","cause":"Kodr treated an older optional create flag as a required part of the OpenShell command surface, while the current CLI removed it.","fix":"Probe sandbox create help and pass --no-bootstrap only when the installed CLI advertises support."}
{"phase":"60","example":"phase-60/openshell-slugify","symptom":"The first isolated Kodr example run against OpenShell reached verification but failed after the local Nemotron model proposed zero files.","cause":"The model returned an informational response and scratchpad without a file proposal, so node --test discovered zero tests.","fix":"Do not hand-fix the example. Preserve the run artifacts as evidence that OpenShell execution and cleanup worked while verification rejected an empty generated result."}
{"phase":"84","example":"linkrot","symptom":"A --subagent-stages run (Sonnet planner, local Nemotron implementer) applied only package.json/README/.gitignore and echoed back the context files prompt.md and cmd.sh as deliverables; no src/ or test/ files were written, so verification reported 0 tests.","cause":"The tiny local implementer could not produce a multi-file project in one proposal and treated files it saw in workspace context as outputs to recreate. --heal was passed but the subagent path returned before runHealingIfNeeded, so no repair ran.","fix":"Protect the active --prompt-file from writes in prepareChanges, and wire runHealingIfNeeded into the --subagent-stages path so failed verification triggers the bounded repair loop."}
{"phase":"84","symptom":"orchestration test \"runs deterministic verification...\" intermittently failed (result.ok false) only under full-suite load.","cause":"The test options() helper set timeoutMs:1000, but the real node --test verification child can exceed 1s when many tests run concurrently, tripping the verification timeout.","fix":"Raise the orchestration test timeoutMs to 5000ms. Not a product bug; the production default model timeout is 600000ms."}
{"phase":"84","example":"linkrot","symptom":"Second linkrot run: manifest extraction correctly found all 13 planned files, but the implementer loop stopped after one pass when the implementer returned an OK proposal with zero files and an intention-only message; nothing was written and the reviewer blocked.","cause":"The file-by-file loop treated any no-progress pass (including an empty first proposal) as a stall and broke immediately, so a weak model that warms up with an intention never got a second turn.","fix":"Tolerate a bounded streak of no-progress passes (MAX_NO_PROGRESS_PASSES=2) instead of stopping on the first, raise MAX_IMPLEMENTER_PASSES to 8 for multi-file manifests, and harden the re-prompt against intention-only replies."}
{"phase":"84","example":"linkrot","symptom":"Third linkrot run: the implementer delivered all 9 files (2 passes) and marked installed, but the reviewer (local nemotron) timed out after 600000ms; the thrown error crashed runSubagentStages, reported the run as failed, and the error path overwrote writes.json/install.json/tests.json with null even though the work was on disk.","cause":"The reviewer model call was awaited without a guard, so a timeout/error propagated and aborted the whole run. The reviewer is advisory but was able to discard a successful implement/install/verify.","fix":"Wrap the reviewer call; on error synthesize an unavailable reviewer result (pass:false, unavailable:true) and base run ok on deterministic verification so the reviewer can no longer crash or silently fail the run."}
{"phase":"84","example":"linkrot","symptom":"--install failed with `npm ci` EUSAGE: lock file marked@13.0.3 did not satisfy package.json marked@14.x.","cause":"A stale package-lock.json from a prior run was in the workspace, and chooseDependencyInstallCommand picks npm ci whenever a lockfile exists. npm ci is strict and refuses when the lock and package.json are out of sync. The model did not choose the command; the harness heuristic did.","fix":"When an auto-chosen npm ci fails (and did not time out), fall back to npm install, which regenerates the lockfile. Explicit npm ci stays strict; record fallbackFrom/fallbackReason in install.json."}
{"phase":"86","symptom":"Focused tests failed after loop-budget snapshots gained zero-valued cacheReadTokens/cacheWriteTokens/cachedTokens fields.","cause":"The initial cache counter plumbing exposed internal zero counters in every loop budget artifact, creating noisy artifact churn for runs with no cache usage.","fix":"Keep zero counters internal and include cache fields in snapshots and summaries only when providers report non-zero cache usage."}
{"phase":"84","example":"linkrot","symptom":"A failed linkrot run with --heal packed phantom repair-context files such as src/linkrot/test/extract.test.mjs before the real test/extract.test.mjs path.","cause":"The repair failure-path scanner matched relative path prefixes inside absolute stack-trace paths, so /Users/paul/src/linkrot/test/... produced an extra src/linkrot/test/... candidate that did not exist in the workspace.","fix":"Normalize failure-path candidates against cwd, prefer existing files, and drop non-existent suffix guesses when the real failing path exists."}
{"phase":"86","example":"linkrot","symptom":"Sonnet subagent raw responses reported non-zero cache reads and writes, but the top-level subagent run summary and CLI usage line omitted cache counters.","cause":"src/orchestration.mjs had a separate mergeLoopBudgets implementation that summed prompt/completion/total tokens and cost only, without normalizing prompt_tokens_details cache fields.","fix":"Normalize cache fields while aggregating subagent responses and omit cache fields only when the aggregate is zero."}
{"phase":"60","symptom":"OpenShell rejected a sandbox exec command argument containing literal newline characters during a real network-denial probe.","likelyCause":"The openshell 0.0.56 gRPC exec surface rejects command arguments with newline or carriage return characters.","fix":"Use newline-free command arguments for sandbox exec; pass complex payloads through stdin or files in future skill execution work.","date":"2026-06-08"}
{"phase":"71","model":"nvidia/nemotron-3-nano-omni","symptom":"A staged self-dev run produced a useful plan scratchpad but then repeated intention-only no-progress turns until StagedIncompleteError.","cause":"Local tool-call requests were sent with strict response_format, biasing the model toward final JSON instead of native tool calls or concrete patches.","fix":"Omit strict response_format for local native-tool requests and preserve the failing .kodr-phase71-staged artifact for diagnosis.","date":"2026-06-08"}
{"phase":"71","model":"qwen/qwen3.6-35b-a3b","symptom":"The planning run response contained a useful scratchpad but scratchpad.md was empty and proposalFound=false.","cause":"The local model embedded multiline markdown directly inside a JSON string, making the response invalid JSON despite the visible envelope shape.","fix":"Use the recorded model response.md as the prior scratchpad source for the acceptance follow-up, and record the failure for future envelope/scratchpad recovery work.","date":"2026-06-08"}
{"phase":"71","model":"qwen/qwen3.6-35b-a3b","symptom":"Staged and broad execution runs exhausted the tool-loop turn budget before normal conversation artifacts were finalized.","cause":"After local tool calls were enabled, unconstrained self-dev prompts could spend every turn exploring tools and never reach a final proposal.","fix":"Use a narrower two-run acceptance flow: one inspection-backed planning turn followed by one read-backed execution turn with prior scratchpad.","date":"2026-06-08"}
{"phase":"93","model":"openai/gpt-oss-20b","symptom":"Model emitted internal structured-output control tokens as literal content: `<|channel|>final <|constrain|>json<|message|>` before the JSON body, causing extractProposal to see a manifest-format file entry with no content field and throw ProposalValidationError.","cause":"gpt-oss-20b uses special tokens to signal channel/constraint mode; these leak into the content field of the API response.","fix":"Strip leading <|token|>text preamble from firstAssistantMessage before JSON extraction.","date":"2026-06-09"}
{"phase":"93","model":"openai/gpt-oss-20b","symptom":"TUI continuation turns after a --subagent-stages run output a planner manifest JSON instead of a file proposal, because the session history included all planner/file-author/reviewer messages with their subagent-role system messages, causing the model to mimic the planner format.","cause":"runSubagentStages concatenated all agent messages into the session; subsequent standard-loop continuation turns inherited the full multi-persona history.","fix":"Replace concatenated agent messages with a synthetic user+assistant pair that summarizes what was written, so continuation turns see a clean single-agent exchange.","date":"2026-06-09"}
{"phase":"85","symptom":"HTTP runs submitted with install:true never ran the dependency install step; install.json was absent and verification failed on missing node_modules.","cause":"The route mapped the body field onto options.install, but the internal option is options.installDependencies; the unknown key was silently ignored by the run path.","fix":"Map install -> installDependencies in createRunTurnOptions and add a field-mapping test asserting every accepted HTTP body field lands on the correct typed internal option.","date":"2026-06-11"}
{"phase":"94","symptom":"Full-suite npm test intermittently failed healing.test.mjs 'repairs a failing write with explicit apply' while the same test passed 3/3 in isolation.","cause":"Pre-existing flake (confirmed by stashing phase 94 changes): the test gave node --check a 1000ms timeout, and under full-suite process load the spawn can exceed it, so the final verification reported a timeout and healed=false.","fix":"Raise that test's verification timeouts to 5000ms; the deliberate 1000ms hung-repair-turn timeout in the same file is unchanged.","date":"2026-06-11"}
{"phase":"97","symptom":"Auto-heal integration tests failed with 'Invalid --test command: Command is not allowlisted' because tests used shell scripts in temp dirs.","cause":"parseVerificationCommand allowlists only npm test, npm run test, node --test, node --test <file>, and node --check <file>. Shell scripts in /tmp are never allowed.","fix":"Rewrite heal integration tests to use 'node --check bad.mjs' with a written-by-proposal broken-then-fixed .mjs file, matching the pattern already used by the existing heal test.","date":"2026-06-11"}
{"phase":"97","symptom":"Numerous existing tests broke when tools defaulted to 'auto' and resolved to true from the qwen profile: length-continuation and response_format tests called completeWithToolCalls instead of completeWithContinuations, and --show-context triggered inspection instead of file listing.","cause":"Changing the default from false to 'auto' (which resolves to true for qwen) changed the code path for all tests that didn't explicitly set --tools or --no-tools.","fix":"Add --no-tools to the 4 tests that specifically exercise non-tools behaviors, and --no-inspect-context to the --show-context test that expects the file-listing path.","date":"2026-06-11"}
{"phase":"99","symptom":"The phase 53 external inspector REGISTRY invokes gopls/rust-analyzer/typescript-language-server with --json flags those tools do not have, and pyright --outputjson emits diagnostics rather than the normalized index shape, so adaptJsonOutput returns []. Since phase 97 defaulted inspectContext to auto, inspectWithRegistry probes and spawns any installed entries with these bogus arguments on every run, and the failures are silently skipped.","cause":"Phase 53 deliberately tested registry shape with fake commands only; the placeholder entries with invented CLI semantics were never validated against the real tools, which speak LSP over stdio rather than one-shot JSON CLIs.","fix":"Discovered while rewriting the phase 99 spec. Phase 99 replaces the invented entries with a real stdio LSP adapter and adds a registry-hygiene test so no default entry can claim an interface its tool does not document.","date":"2026-06-11"}
{"phase":"101","symptom":"post-write-sensor.mjs imported { performance } from node:perf_hooks, causing a test failure.","likely_cause":"Node 24 provides performance as a global; the named import created a redundant binding that confused the module loader in some contexts.","fix":"Removed the import; use the global performance object."}
{"phase":"101","symptom":"renderDiagnosticsForModel received files with lspDiagnostics field but expected diagnostics field, producing empty output.","likely_cause":"runLspInspector returns files with lspDiagnostics as the field name; the harness renderer used a different name.","fix":"Added a normalization step in inspectChangedFiles that maps lspDiagnostics to diagnostics."}
{"phase":"102","symptom":"healing.mjs:151 calls prepareChanges with no try/catch — a mismatched patch in a repair turn crashes the entire healing loop.","likely_cause":"preparePatches threw SafeWriteError on match failures, and the healing loop never caught it because the original code assumed all patches would match.","fix":"preparePatches now collects failures into failedPatches instead of throwing. The healing loop continues and records failed patches in turn artifacts."}
{"phase":"102","symptom":"NEXT.md phase description assumed unified-diff input format existed, but the codebase only had search/replace patches in JSON.","likely_cause":"The strategic review at phase 99 described the desired direction without verifying the actual code paths.","fix":"Phase re-targeted: tolerant patch application + retry loop + new blocks format + format as model-profile field."}
{"phase":"109-dogfood","symptom":"Brownfield dogfood run (qwen3.6, tools mode): model reads files, correctly diagnoses the planted bug, then attempts the edit via run_command (sed -i, node --eval) which the allowlist correctly blocks, then re-runs the failing test until turn_budget_exhausted. Run hard-fails with zero writes.","likely_cause":"Tools mode exposes read/inspect/run tools but no write path, and nothing steers the model back to the proposal envelope when it attempts writes via run_command. The phase 103 no-progress detection does not cover repeated identical tool calls inside completeWithToolCalls.","fix":"Open. Candidates: inject a corrective tool result when a blocked command looks like a write attempt; trip no-progress on repeated identical tool calls; force a final no-tools proposal turn when the budget is nearly exhausted."}
{"phase":"109-dogfood","symptom":"When completeWithToolCalls exhausts the turn budget, LoopBudgetError propagates as a fatal run error and all accumulated conversation turns and usage are lost: raw-response.json has responses: [], summary has responseCount 0 and usage null, despite 8 real model calls. A Sonnet test operator misdiagnosed this as 'fails before any HTTP request with maxTurns=0'.","likely_cause":"The runPrompt error path only serializes the error; the per-turn messages/usage live inside completeWithToolCalls locals and are never persisted on the throw path.","fix":"Open. Budget exhaustion should be a graceful stop that salvages messages, responses, and usage into artifacts (and likely still attempts proposal extraction from the last assistant text)."}
{"phase":"109-dogfood","symptom":".kodr/last-run is only written on successful runs, so 'kodr why' with no argument fails exactly when a run just failed — the moment forensics is most needed.","likely_cause":"The last-run pointer write lives on the success path only; writeRunFailure does not update it.","fix":"Open. Write last-run on both success and failure paths."}
{"phase":"109-dogfood","symptom":"kodr why on a failed run shows 'Model Call: ok' with turns=?, tokens=? even when responseCount is 0 and error.json holds a LoopBudgetError.","likely_cause":"buildCausalStory classifies the Model Call step without consulting summary.ok or error.json.","fix":"Open. Model Call step should go fail/warn when summary.ok is false and the error originated in the model loop."}
{"phase":"109-dogfood","symptom":"summary.workspaceFileCount is always 0 in tools mode for every run, failed or successful.","likely_cause":"buildWorkspaceContext sets files: [] in the tools path (the listing moves to fileMap) and the summary only counts context.files.","fix":"Open. Count fileMap entries when present."}
{"phase":"109-dogfood","symptom":"Greenfield dogfood runs (both tools and no-tools): the repair turn's model call reliably hits the full 600s timeout and dies silently, while the main proposal call finishes in ~30s. Heal loop ends as timeout with no retry, no reduced-context fallback, and no user-facing warning.","likely_cause":"Repair prompts pack failing-test output plus file contents and are far larger than the main prompt; turnTimeoutMs defaults to the same 600000ms as the main call, and one slow local completion eats the whole heal budget.","fix":"Open. Candidates: cap repair prompt size, stream-abort on stall rather than wall clock, or surface the timeout clearly and skip to the next repair strategy."}
{"phase":"109-dogfood","symptom":"kodr why .kodr/runs/<id> (relative path) resolves to <cwd>/.kodr/runs/.kodr/runs/<id>, then renders a full story with every step 'skip' instead of erroring that the run dir does not exist.","likely_cause":"resolveRunDir treats any non-absolute argument as a bare run ID and joins it under .kodr/runs/; loadRunAnalysis returns all-null for a missing dir and buildCausalStory happily renders it.","fix":"Open. Detect path-like arguments (contains a separator) and resolve against cwd; error out when the resolved dir has no artifacts at all."}
{"phase":"109-dogfood","symptom":"repair-context.json includes a ghost file test/wordfreq.mjs with empty content that never existed on disk, alongside the real test/wordfreq.test.mjs.","likely_cause":"buildRepairContext appears to derive candidate paths from test failure output (the test file imports ../wordfreq.mjs, and runner output mentions wordfreq.mjs) and includes unreadable paths as empty strings instead of skipping them.","fix":"Open. Verify existence before including a file in repair context; never present empty-content phantom files to the repair model."}
{"phase":"109-dogfood","symptom":"Repair turn wrote wordfreq.mjs while the failing tests lived in test/wordfreq.test.mjs; wrongPathSiblings flagged it correctly but the write was applied anyway, verification failed again, and the next repair turn timed out.","likely_cause":"Phase 103 wrong-path detection is advisory (prompt-side warning on the next turn) rather than a gate at apply time. Possibly by design, but it spends heal budget on detected-wrong writes.","fix":"Open question for repair-pressure design rather than a clear bug; consider gating or down-weighting detected wrong-path repairs."}
{"phase":"109","symptom":"Re-run of the brownfield dogfood test after the phase 109 fixes: kodr fixed the planted bug end-to-end. The model spent 7 turns on tool calls (the same behavior that previously burned the whole budget), then the forced final no-tools turn produced a correct one-line patch; apply and node --test verification passed. finishReasons=[tool_calls x7, stop], 18,560 tokens, workspaceFileCount now 4 instead of 0.","likely_cause":"Not a failure — recorded as the resolution evidence for the 109-dogfood entries above.","fix":"F1 final-turn forcing converted budget exhaustion into a final proposal; evidence at kodr-testing/phase-109/brownfield-fix-2/.kodr/runs/2026-06-12T03-16-34.079Z."}
{"phase":"109","symptom":"npm test hangs forever: every test in lsp-client.test.mjs passes, but the file's process never exits and the suite (run with --test-timeout=0) waits indefinitely. User had observed the same hang previously.","likely_cause":"runLspInspector spawns the LSP server child, then awaits client.initialize() under withTimeout; when initialize times out the error propagates without killing the child. The orphaned hangOnInit fake server keeps the test process's event loop alive. Same leak would orphan a real hanging language server in lsp:auto runs.","fix":"runLspInspector now kills the client before rethrowing initialize failures. Verified: node --test test/lsp-client.test.mjs exits cleanly."}
{"phase":"109","symptom":"watcher.test.mjs 'detects a file change' fails deterministically on this machine: changes contained only the watch root's own directory basename, not hello.js.","likely_cause":"macOS FSEvents delivers an event for the freshly created watch root itself; the 50ms debounce flushes that batch first and the test asserted on the first nonempty batch. Pre-existing at HEAD, unrelated to phase 109 code changes.","fix":"Test now waits for the specific file to appear in changes instead of any first batch. Product behavior unaffected — a spurious root event only triggers an extra test run in kodr watch."}
{"phase":"109","symptom":"tui.test.mjs 'prints streamed chunks' failed in the user's shell but passed elsewhere: ANSI codes appeared between the assistant> label and the message, breaking label-spanning regex assertions.","likely_cause":"createTuiView defaults env to process.env, and the user's shell exports FORCE_COLOR, which force-enables color even on non-TTY capture streams. This was the only TUI test asserting across the label boundary without pinning env (the dedicated color tests pass env explicitly).","fix":"Test now passes env: {} so ambient FORCE_COLOR cannot affect it. Verified by running the file under FORCE_COLOR=1."}
{"phase":"109","symptom":"While simplifying the npm test script after the examples directory moved out of the repo: node --test (Node 24.16) does not accept directory arguments — 'node --test test/' execs the directory as a script and fails with MODULE_NOT_FOUND, despite documentation suggesting directory support.","likely_cause":"Node test runner positional args are treated as files in this version; only glob expansion works reliably.","fix":"npm test uses shell globs: node --test test/*.test.mjs packages/repomap/test/*.test.mjs. The repomap package tests (32) now run in the main suite, which also guards the src/repomap <-> packages/repomap sync rule from phase 108."}
{"phase":"110","symptom":"kodr why showed the Healing step as skip with healingTurns=0 for every run that actually had repair turns.","likely_cause":"repairs.json is an object ({healed, stopReason, repairs: [...]}) but buildCausalStory's healing step treated the whole document as the repairs array, so real runs always produced an empty list. Test fixtures happened to use bare arrays, hiding the mismatch.","fix":"Healing step now accepts both shapes and reads the nested repairs array; fixed alongside the D2 timeout detail work in phase 110."}
{"phase":"110","symptom":"D4 replay of the round-1 timed-out repair requests: all three variants completed in 12-27s — the 600s stall did not reproduce. But the no-tools variant WITH response_format (strict json_schema) returned finish_reason=stop with empty content: all 709 completion tokens were reasoning tokens. The same prompt without response_format produced a 2,230-char proposal in 26s.","likely_cause":"LM Studio structured-output enforcement interacts badly with qwen3.6's reasoning mode: constrained decoding can consume the budget as reasoning and emit no content (and plausibly spiraled for 600s in round 1). Evidence: /tmp/ab_{a,b,c}_resp.json replay of phase-109 greenfield repair prompts.","fix":"Open. Candidate: when a structured-output completion returns empty content alongside reasoning tokens, retry once without response_format; longer-term, reconsider json_schema enforcement for reasoning-model profiles."}
{"phase":"110","symptom":"D4 heal trial (tools mode): prompted to write a README in a workspace with a planted failing test, the model made 2 tool-call turns then returned a 2-char final answer ({}) instead of a proposal envelope; run failed at proposal extraction, so verification and healing never ran.","likely_cause":"In tools mode the model treats a documentation task plus visible failing tests as unresolvable and emits an empty envelope. Bare kodr why on the failed run worked (phase 109 F3) and correctly pinned the failure to Proposal Extraction (responseChars=2).","fix":"Open — small-model envelope discipline for non-code tasks; for the heal trial itself, rerun with --no-tools (evidence: kodr-testing/phase-110/heal-trial-1/.kodr/runs/2026-06-12T04-39-30.404Z)."}
{"phase":"110","symptom":"D4 heal trial 3: with the imported source file finally in repair context (D6), the repair turn stalled past the new 240s cap — reproducing round 1's 600s timeouts. D2 surfaced it exactly as designed: 'repair turn timed out after 240s (limit 240s). Raise with --repair-timeout-ms.'","likely_cause":"Strict json_schema response_format on repair turns. Direct A/B on the identical stalled prompt: without schema, 6s and a correct fix; with schema, empty content (all tokens to reasoning). LM Studio constrained decoding + qwen3.6 reasoning either returns empty or grinds.","fix":"runHealingIfNeeded strips responseFormat from repair turns; the repair prompt demands the JSON envelope in text and extraction is defensive. Evidence: /tmp/heal_ab_{plain,schema}_resp.json and kodr-testing/phase-110/heal-trial-3."}
{"phase":"110","symptom":"D4 heal trial 4: the repair model produced the exactly correct patch to src/math.mjs in 5.8s, the patch applied, the file on disk was fixed — and the loop reported not healed (no-progress-exhausted), leaving a healed workspace it could not see.","likely_cause":"The phase-103 wrong-path heuristic treated 'did not touch the failing test file' as wrong and skipped re-verification entirely on such turns. But fixing the source file when the test fails is the normal repair shape — the failing path is the symptom, not the bug. The heuristic overrode measurement.","fix":"Wrong-path is now judged against the full repair-context set (failing tests + siblings + D6 imported sources), verification runs unconditionally after applied writes, and the D3 pre-apply gate was removed for the same reason — heuristics must never block measurement. Evidence: kodr-testing/phase-110/heal-trial-4."}
{"phase":"111-dogfood","symptom":"Round-2 operator subagent backgrounded both kodr runs and ended its turn; the kodr processes died with the subagent session, leaving both workspaces with a one-line run.log and no .kodr artifacts. It also launched both runs concurrently against the single LM Studio instance.","likely_cause":"Subagent shells do not outlive the subagent's final message; background work must be awaited within the agent's own turn. Concurrent runs against one local model also contend for the queue — a plausible contributor to round 1's unreproducible 600s repair stalls (that operator also ran tasks in parallel).","fix":"Process note: dogfood runs are executed from the parent session (or awaited in-turn by the operator agent); subagents handle analysis and testing of completed artifacts. Recorded for AGENTS.md consideration."}
{"phase":"111-dogfood","symptom":"Round-2 dominant failure mode, hit in BOTH tasks: qwen3.6 plans a complete correct implementation in reasoning tokens (up to 4,117) then emits content of just two newlines on a finish_reason=stop turn. Greenfield: repair turn 2 died as invalid_proposal. Brownfield: final turn after tool-call file reads produced 2 response chars; ProposalMissingError, no writes. This occurs WITHOUT response_format — broader than the phase-110 json_schema finding.","likely_cause":"qwen3.6 reasoning-mode termination: the model treats chain-of-thought as terminal output. Not truncation (stop, not length), not schema-constrained decode.","fix":"Open. Highest-value mitigation per analysis: when a turn ends with stop and near-empty content (<10 chars) after substantive reasoning/tool context, send one nudge retry ('your response was empty — output the JSON proposal now'). Also surface 'Proposal: MISSING — response was empty (N chars)' in run output. Evidence: phase-111/{greenfield-logstats-1,brownfield-wordfreq-feature-1} raw-response.json."}
{"phase":"111-dogfood","symptom":"Greenfield logstats: the model emitted one files[] object containing TWO duplicate 'path' keys (logstats.mjs and test/logstats.test.mjs merged into one object); JSON.parse silently kept the last, so the CLI file was never written and every integration test failed on undefined stdout.","likely_cause":"Model envelope malformation plus JSON.parse duplicate-key semantics: the harness cannot see dropped keys after parsing.","fix":"Open. Candidate: detect duplicate 'path'/'content' keys in the raw files/patches JSON text before parse (cheap regex over the extracted block) and warn or repair-prompt on it. Evidence: phase-111/greenfield-logstats-1 raw-response.json vs writes.json (1 write where 2 files were intended)."}
{"phase":"111-dogfood","symptom":"Scripted TUI session (printf lines | kodr tui): /status rendered correctly, but the prompt turn produced no output at all — no 'request model=' line, no run dir, no .kodr/runs created — and the session exited 0. The buffered /review, /reject, /status, /quit lines after the prompt were also never echoed.","likely_cause":"Piped stdin lines are not serialized against in-flight turns: the readline interface appears to deliver/close before or while the async turn handler runs, and EOF/quit tears the session down, silently abandoning the pending model call. TUI tests pass because mock channels resolve instantly.","fix":"Open. The TUI line loop should queue input lines while a turn is in flight and only exit on EOF after the active turn settles. Evidence: kodr-testing/phase-111/tui-session-1/tui-session.log (exit 0, no run artifacts)."}
{"phase":"111-dogfood","symptom":"kodr bench with the proposal-style todo-cli suite reported 0/0 (score 0.00) for all 7 served models in ~2 minutes — no model calls happened, the bench run dir is empty — then derived and saved a routing table from the all-zero scores (edit -> qwen at score 0.00) with no warning. The embedding model was benched alongside chat models.","likely_cause":"Bench routes every suite through runWorkspaceSuite, which only executes workspace-fixture cases; proposal-style cases are silently excluded, and bench filters results to status==='ran' so the empty set scores 0/0. Routing derivation does not distinguish 'measured 0' from 'measured nothing', and discoverModels does not filter non-chat (embedding) models.","fix":"Open. Bench should run proposal cases too or refuse the suite with a clear error; a routing table must not be derived/saved when no cases ran; discoverModels should exclude embedding models. Evidence: /tmp/bench-r2.log, empty .kodr/runs/2026-06-12T05-35-29.492Z, .kodr/routing.json."}
{"phase":"111-dogfood","symptom":"kodr eval --suite evals/todo-cli.json run directly: the case executed but scored 0/4 with 'missing from proposal: src/cli.mjs...' — the model produced a proposal without the files, consistent with the round-2 reasoning-then-silence/empty-envelope mode on qwen3.6.","likely_cause":"Same model failure mode as the example tasks; eval surfaced it correctly with per-assertion detail.","fix":"Covered by the empty-final-turn recovery candidate in NEXT.md. Evidence: .kodr/runs/2026-06-12T05-36-12.026Z/eval-results.json."}
{"phase":"111","symptom":"gemma-4 multi-block narration: six ```json blocks emitted, old regex extractor matched only three due to fence pairing bug. Block 1 was a planning envelope with files:[], blocking real file content in blocks 2-3 from reaching the proposal.","likely_cause":"The regex /```(?:json)?\\s*([\\s\\S]*?)```/giu uses non-line-anchored fences and a lazy match. When six ```json lines appear with no closing ``` between them, the alternation paired open-with-next-open instead of open-with-close. Three 'blocks' of mixed content were extracted instead of six real blocks.","fix":"Replaced regex with a line-anchored state machine: a fence-open line is /^```json\\s*$/; a fence-close is /^```\\s*$/. Consecutive ```json lines without a closing ``` are treated as starting new blocks. All six blocks now extracted. Evidence: ~/src/kodr-testing/phase-111/gemma-smoke-1 response.md."}
{"phase":"111","symptom":"braceWalk throws JsonExtractionError ('Unclosed JSON candidate', 'Mismatched JSON delimiters') from inside candidateTexts, outside the per-candidate try/catch in extractJson/extractProposal. One malformed brace region aborted all candidate enumeration, discarding valid fenced blocks. Reproduced in the gemma repair turn (repairs.json: stopReason invalid_proposal, error Unclosed JSON candidate).","likely_cause":"candidateTexts called braceWalk once and propagated its throw directly. The per-candidate try/catch in extractJson was never reached for fenced blocks if braceWalk threw first.","fix":"candidateTexts now catches braceWalk errors and retries from the next open brace (bounded by MAX_BRACE_RETRIES=16). Fenced blocks are always enumerated regardless of braceWalk outcome."}
{"phase":"111","symptom":"Nested duplicate path key in files[] array silently dropped a file: greenfield logstats run had files:[{path:logstats.mjs, path:test/logstats.test.mjs, content:...}], JSON.parse kept the last key value, first file never written, all integration tests failed.","likely_cause":"assertNoDuplicateTopLevelKeys only walked depth-1 object keys. Nested object keys (inside files[], patches[], etc.) were never checked.","fix":"Renamed to assertNoDuplicateKeys, now maintains a stack of per-object key sets through the walker. Every object at any depth gets its own Set; duplicate keys at any level throw 'Duplicate JSON key: <key>'."}
{"phase":"111","symptom":"Live gemma-4 validation re-run after the phase-111 extraction fixes still failed (finish_stop, 4468 response chars, zero files written): the model emitted a literal pseudo-token <|\"|> in place of escaped/closing quotes inside JSON string values, 7 occurrences, corrupting every one of the five fenced envelopes. Confirmed present in raw-response.json, so it is genuine model output, not a kodr rendering artifact. Evidence: ~/src/kodr-testing/phase-111/gemma-smoke-2/.kodr/runs/2026-06-12T06-54-00.966Z/.","likely_cause":"google/gemma-4-26b-a4b (LM Studio build) decode artifact: the model substitutes a special-token-shaped literal for quote escapes when writing JSON inside content. Deterministic in shape (<|\"|> where \\\" or \" belongs).","fix":"Not fixed in phase 111 (found by its validation run). Candidate: targeted decode-artifact repair in repairJsonText (<|\"|> -> \"), behind evidence from more gemma runs. Also noticed: a substantial-content finish_stop with no extractable proposal does not engage the repair loop the way invalid_proposal does — only the E4 empty-content nudge covers the no-proposal stop path."}
{"phase":"112","symptom":"json_schema strict constrained decoding stalls google/gemma-4-26b-a4b on LM Studio: variant C (no tools, json_schema) stalled >300s and dropped connection — 13× the unconstrained latency with no response. Variant E (tools + json_schema) identical result.","likely_cause":"Reasoning-model constrained decode: gemma-4 populates reasoning_content (2,209 chars in variant B), suggesting it is a reasoning-channel model. Constrained decode conflicts with the reasoning chain, producing an infinite-loop or runaway token stream the server eventually drops. Same pattern as qwen3.6 in phase 110.","fix":"S1: structuredOutput: 'none' is the measured default for all local/LM Studio profiles. The json_schema path is not used for any local model until re-measured to be safe."}
{"phase":"112","symptom":"LM Studio rejects json_object with HTTP 400: 'response_format.type must be json_schema or text'. Variant D confirmed.","likely_cause":"LM Studio API does not implement json_object mode — only json_schema and text are valid values.","fix":"S1: json_object is excluded from LM Studio-provider profiles. loadModelProfiles validates configured profiles at load time and throws loudly if json_object is used for local/lmstudio providers."}
{"phase":"112","symptom":"Final-turn forcing in completeWithToolCalls silently reattached json_schema on local runs: F1 removes tools from the body, which caused the old shouldOmitResponseFormat (local + tools → omit) to stop matching, so response_format was attached on exactly the turn that matters most for reasoning models.","likely_cause":"shouldOmitResponseFormat was a body-level heuristic (checks body.tools) rather than a profile capability. F1's body transformation subverted the heuristic without any of the callers noticing.","fix":"S2: deleted shouldOmitResponseFormat. The profile's structuredOutput mode is the single source of truth and applies identically to all turn types (main, forced final, repair, E4 nudge)."}
{"phase":"112","symptom":"Two consecutive kodr runs timed out at 600000ms with zero bytes received (gemma-smoke-3 runs 1-2), while a tiny curl request succeeded between them and the exact same raw request body streamed tokens instantly via curl afterwards. Followed two A/B json_schema variants that were aborted client-side at ~300s (undici headers timeout) while LM Studio kept the constrained generations grinding server-side.","likely_cause":"LM Studio left in a degraded state by the abandoned constrained-decode generations; large tool-enabled requests stalled with no first token while trivial requests squeezed through. Recovered without intervention. Secondary observation: kodr waits the full timeoutMs with zero bytes received - there is no time-to-first-token deadline, so a stalled server consumes the entire 600s budget before failing.","fix":"None needed for the server (transient). Harness candidate recorded in NEXT.md: a separate first-token timeout so zero-byte stalls fail fast and can retry. Third attempt succeeded fully: first green gemma end-to-end run - 5 fenced blocks merged into one proposal (phase 111 E2/E5), zero quote-token artifacts, 2 writes, node --test 6/6, exit 0. Evidence: ~/src/kodr-testing/phase-112/gemma-smoke-3/."}
{"phase":"113-dogfood","symptom":"Zero-byte 600s stalls reproduced and root-caused: identical request body returns first token instantly with stream:true but hangs indefinitely with stream:false on LM Studio (gemma-4 MLX). kodr couples wire streaming to display: options.stream==='auto' resolves to stdout.isTTY && !json (app.mjs ~1168), so every redirected/piped/programmatic run sends non-streaming requests and inherits the hang. All four 600s zero-byte stalls across smoke-1/smoke-3/round-3 happened in non-TTY runs.","likely_cause":"LM Studio non-streaming completion path hangs intermittently on large tool-enabled requests (server-side bug or slot poisoning from abandoned generations). kodr's transport choice should never depend on whether a human is watching: serve, watch, subagent-driven, and logged runs are all non-TTY.","fix":"Workaround: pass --stream explicitly. Real fix queued in NEXT.md: stream-first transport - always stream on the wire (parser already complete, include_usage already requested), keep TTY only for display rendering; add first-token deadline."}
{"phase":"113-dogfood","symptom":"Goal-substitution heal: greenfield logstats run reported ok:true/healed while the requested CLI was never written. Chain: gemma emitted 17 quote pseudo-tokens corrupting all file-carrying blocks -> merged proposal had 5 valid narration envelopes, zero files -> node --test found 0 tests -> heal engaged with no task anchor (repair prompt had only the test failure, failurePaths empty, empty workspace) -> repair invented index.js+test.js (trivial CommonJS countOccurrences with its own passing test) -> verification green -> healed. Evidence: ~/src/kodr-testing/phase-113/greenfield-logstats-1/.","likely_cause":"Three stacked gaps: (1) repair prompt does not inherit the original user task, so the model cannot know what should exist; (2) zero-files-written + zero-tests-found is an extraction failure being misrouted into the heal path; (3) a repair that only creates paths never mentioned in the task or prior proposal is not flagged as suspect.","fix":"Queued in NEXT.md as heal task anchoring. Ranked mitigations from round-3 analysis: inherit original prompt in repair context; route writeCount=0+tests=0 to extraction retry not heal; treat never-mentioned-path-only repairs as suspect."}
{"phase":"113-dogfood","symptom":"Phase 112 S3 decode-artifact rule is correct but insufficient: in the logstats response, gemma collapsed key-closing-quote+colon+value-opening-quote into 'content:<|\"|>' (role B, 3 occurrences, one per file block) in addition to plain closing-quote substitution (role A, 14 occurrences). Blanket <|\"|> -> \" replacement yields '\"content:\"import...' which is still invalid JSON, so every file-carrying block remained unparseable even after S3; only narration blocks survived. Verified empirically against response.md with the real extractor (analyst subagent initially overclaimed that blanket replacement rescues all blocks; it does not).","likely_cause":"The pseudo-token swallows adjacent structural quotes, not just itself: '\"content\":\"' degrades to '\"content:<|\"|>'.","fix":"Queued: structural decode-artifact rule '\"<key>:<|\"|>' -> '\"<key>\":\"' (regex over unterminated key followed by :<|\"|>), applied before the plain substitution. Fixture: phase-113 greenfield-logstats-1 response.md."}
{"phase":"113-dogfood","symptom":"GUI-loaded gemma-4 had context_length 8192 (parallel 4) while kodr's profile assumes 32768 - discovered via LM Studio management API GET /api/v1/models, which reports loaded_instances[].config. Reloaded at 32768 via POST /api/v1/models/load in 8s.","likely_cause":"LM Studio GUI default context on load does not match kodr profile assumptions; kodr has no way to notice the mismatch.","fix":"Candidate: kodr probe could read /api/v1/models (when the management API is present) and warn when loaded context_length is below the profile contextWindow."}
{"phase":"113","symptom":"First-token timeout timer (firstTokenTimeoutMs) was unref-ed in the Promise.race, causing it to not fire in test environments where the stall server holds the connection and the timer is the only ref keeping the event loop alive.","likely_cause":"setTimeout(...).unref() makes the timer not prevent Node.js exit. In tests where the I/O is the only other active handle, unref-ed timers can be silently skipped.","fix":"Removed .unref() from the first-token timer. The overall request timer (requestWithNodeHttp) remains unref-ed since it is bounded by the I/O event.","date":"2026-06-12"}
{"phase":"113-validation","symptom":"Live validation: gemma-4 stalled at the SSE layer on both the initial attempt and the automatic retry (HTTP 200, phase stream-read, zero/one chunk then silence), so the run consumed the full 600s outer timeout. The first-token deadline fired at 120s and the single retry ran as designed; the retry evidently received a first chunk and then hit an inter-token stall, which the phase spec leaves to the overall timeout. gpt-oss-20b on the identical redirected non-TTY configuration was fully green: wire=stream, TTFT 1064ms, firstTokenRetries 0, surfaced in summary.json and kodr why.","likely_cause":"Server/model-side: gemma-4 MLX on LM Studio intermittently produces no (or one) SSE chunk on large tool-enabled requests; possibly degraded parallel slots after a day of abandoned generations. Not a phase-113 regression - the same configuration succeeded repeatedly in round 3.","fix":"Candidates queued in NEXT.md: inter-chunk idle deadline (a stream that goes silent mid-read should fail faster than the outer timeout); per-model tool-call compatibility could be measured the same way structured output was in phase 112."}
{"phase":"113-validation","symptom":"gpt-oss-20b emitted a single stray quote character before the second files[] element ('},\"{' instead of '},{'), corrupting an otherwise valid envelope; no files written, ProposalMissingError. Manually removing the one character yields a working CLI (graded D overall: functional CLI but its test file imports symbols the implementation never exports).","likely_cause":"Single-token emission error; each local model so far has its own characteristic single-character JSON corruption (gemma: quote pseudo-tokens; gpt-oss: stray quote).","fix":"Extends the Structural Decode-Artifact Rules NEXT.md candidate: position-aware single-character repair between array elements is recoverable in the same pre-parse pass. Evidence: ~/src/kodr-testing/phase-113/transport-validation-gptoss/.kodr/runs/2026-06-12T11-41-44.327Z/response.md byte ~1941."}
{"phase":"114","symptom":"Test 'filters binary files and includes AGENTS.md as instruction context' checked for inspect_symbols in non-tools mode system prompt. After phase 114 moved the tool sentence to the # Tools section (tools mode only), this assert became a false expectation.","likely_cause":"The old contract included the tool sentence in all modes with conditional phrasing ('When native tools are available, use...'). Phase 114 deliberately removed it from non-tools mode.","fix":"Updated assertion: doesNotMatch(inspect_symbols) in non-tools mode; added match(# Behaviours) to confirm new section is present.","date":"2026-06-12"}
{"phase":"114-validation","symptom":"A/B iteration 1 failed its own criterion: gemma regressed from baseline success to a plan-stall - the behaviour line 'Return exactly ONE JSON envelope per response. Never narrate a sequence of JSON blocks' suppressed the multi-block narration that previously carried the code, and gemma returned a single envelope with files:[] (a plan, no implementation). Separately gpt-oss read the new Tools section and hallucinated a write_file tool across 4 turns (prompt tokens 873 -> 8,141).","likely_cause":"Prompt said what not to do (narrate) without saying what the single envelope must contain (the complete work). Tool list resembled a capability menu without stating the write path goes through the envelope.","fix":"Wording tuned: envelope line now demands COMPLETE files/patches and forbids deferring code; Tools block gained 'There is no write or edit tool - all file changes go in the files/patches arrays of your final JSON envelope.' Iteration 2: gemma succeeded with exactly 1 fenced block (baseline 5), 0 decode artifacts, 2 writes, tests green. Lesson recorded: behaviour lines must state the positive contract, not just the prohibition."}
{"phase":"114-validation","symptom":"gpt-oss-20b corrupted the files[] element boundary for the third time in three runs, each a different single character: baseline stray quote ('},\"{'), iteration 1 missing brace, iteration 2 dropped opening brace ('\"},\"path\":' instead of '\"},{\"path\":' at position 2036). The envelope was otherwise complete and valid; one inserted character yields a working two-file proposal. It also continued calling the nonexistent write_file tool (5 calls) despite the explicit 'there is no write tool' prompt line.","likely_cause":"Deterministic single-token emission fault at the object boundary inside arrays; prompt instructions do not reach it. The hallucinated tool calls suggest unknown-tool error feedback is not steering the model back to the envelope.","fix":"Two queued candidates strengthened: Structural Decode-Artifact Rules gains the array-boundary rule ('},\"<key>\":' -> '},{\"<key>\":'); new idea - unknown tool-call error responses should name the envelope as the only write path (steering, like the phase-109 allowlist hint). Evidence: ~/src/kodr-testing/phase-114/ab2-gptoss/."}
{"phase":"114-validation","symptom":"Gemma SSE stall recurred (3 of 4 attempts in the afternoon) and was cleared each time by unloading and reloading the model via the LM Studio management API (8s); the immediately following identical request succeeded.","likely_cause":"Server-side degradation that accumulates over a long session (suspected wedged parallel slots from abandoned generations).","fix":"Operational: reload the model when a stall recurs. Harness candidate already queued (inter-chunk idle deadline) would at least fail fast."}
{"phase":"115","symptom":"Initial R2a replacement was '},{\\'  instead of '},$1', causing double-quote insertion: '},\"(\\{)' pattern with replacement '},{\\'  yields },{\\\" (extra quote before key), so '},\"{\"path\":'  became  '},{\"\"path\":' — still invalid JSON.","likely_cause":"Replacement string '},{\\'  appended an extra \" that was already present in the original text immediately after the match. The captured group { was not used via $1.","fix":"Changed replacement to '},$1' which outputs },{  (just the opening brace, no extra quote). The original text contributes the following \" as the start of the key string.","date":"2026-06-13"}
{"phase":"115-validation","symptom":"mistralai/devstral-small-2-2512 emits tool_calls entries with arguments:\"\" (empty string) instead of \"{}\" on every tool call with no arguments. LM Studio returns HTTP 500 to any request whose conversation history contains such an entry. Every multi-turn devstral run crashed on turn 3 (first time the history with the tool_call was replayed). Confirmed by proxy interception: patching empty string to \"{}\" and replaying the identical request returns HTTP 200.","likely_cause":"devstral-small-2-2512 omits the arguments field (or emits empty string) for zero-argument tool calls rather than the OpenAI-spec-required \"{}\". LM Studio validates outbound history and rejects messages containing arguments:\"\".","fix":"normalizeToolCallArguments() in tool-calls.mjs maps arguments:\"\" and arguments:null/undefined to \"{}\" before the assistant tool_calls message is appended to conversation history. Dispatch already handled the empty-string case via the argsJson || \"{}\" fallback. Artifacts (raw-response.json) preserve the model actual bytes; only the outbound request bodies are normalized.","date":"2026-06-13"}
{"phase":"115-validation","symptom":"After the empty-arguments fix, devstral runs complete 8 turns with no crashes but never produce a proposal: it persistently calls a nonexistent `files` tool (4-5x per run), ignores the R4 unknown-tool steering and the repeat-call detector every time, and ends on turn_budget_exhausted with an empty stop (0 files, 0 fenced blocks). Total final-turn content across the greenfield run: 48 chars.","likely_cause":"devstral-small-2-2512 is trained around a native `files` write tool; its tool-use prior overrides prompt-level steering entirely. The envelope contract is foreign to it.","fix":"None in this phase — model compatibility finding, not a harness bug. Candidates queued in NEXT.md: a devstral-specific profile mapping its `files` tool calls onto the proposal write path, or Mistral-tuned steering wording.","date":"2026-06-13"}
