# cq-nodetest

Greenfield ESM module + node:test generation. Code-quality trap fixture
(phase 124): the model must use the real `node:test` API (`test`, `assert`),
not invented methods like `t.assert()`, and ESM not CommonJS.
