Repair the generated Express/Postgres example so its own npm test no longer crashes with recursive fetch/OOM. Use tools to inspect tests/health.test.js, tests/utils.js, tests/users.test.js, tests/documents.test.js, src/app.js, and README.md. Then produce patches. Specific known issue: a helper named fetch calls fetch recursively; call globalThis.fetch or rename the helper to request. Also remove duplicated README sections. Keep changes scoped to this example.