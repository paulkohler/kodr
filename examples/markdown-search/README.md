# Markdown Search Example

This is a Kodr-generated local Markdown search example.

## Usage

```sh
npm test
node src/cli.mjs <directory> <query>
```

The CLI searches all `.md` files in the given directory for the supplied query and prints matching file paths with a short snippet.

## License

MIT