# Search Notes

This document describes the purpose and usage of the Markdown search example.

- The example indexes `.md` files in a specified directory.
- It supports simple substring search across file contents.
- Results are printed as file path followed by a short snippet.
- The CLI accepts a directory path and a query string as arguments.