# Phase 01: CLI Skeleton

## Goal

Create the smallest tested command surface.

## Build Steps

- [x] Add `package.json`.
- [x] Add `bin/kodr.mjs`.
- [x] Add root `./kodr` wrapper.
- [x] Add `src/app.mjs`.
- [x] Add native `node:test` coverage.

## Done Criteria

- [x] `npm test` passes.
- [x] `./kodr --help` works.
- [x] Default timeout is `600000ms`.
