# Phase 25: CSV Expense Example

## Goal

Use Kodr to generate the next example app: a CSV expense analyzer.

## Scope

- [x] Pick the next-hardest example from the candidate list.
- [x] Add a prompt for the CSV expense analyzer.
- [x] Attempt generation through Kodr and preserve artifacts.
- [x] Apply or complete the app under `examples/csv-expenses`.
- [x] Run the example app tests from its own directory.
- [x] Update Kodr if the trial exposes a harness gap.

## Done Criteria

- [x] `examples/csv-expenses` has a package, source, tests, sample data, and README.
- [x] The example parses CSV with quoted fields and escaped quotes.
- [x] The example groups expenses by month and category.
- [x] Tests cover parsing, validation, grouping, reporting, and CLI output.
- [x] Blog post documents the harness trial.
