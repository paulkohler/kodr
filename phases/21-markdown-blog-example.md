# Phase 21: Markdown Blog Example

## Goal

Use Kodr to generate the next example app: a small Markdown blog generator.

## Scope

- [x] Pick the next-hardest example from the candidate list.
- [x] Add a prompt for the Markdown blog generator.
- [x] Attempt generation through Kodr and preserve failure artifacts.
- [x] Apply the generated app into `examples/markdown-blog`.
- [x] Run the example app tests from its own directory.
- [x] Update Kodr if the trial exposes a harness gap.

## Done Criteria

- [x] `examples/markdown-blog` has a package, source, tests, sample posts, and README.
- [x] The example converts Markdown posts into static HTML under `dist`.
- [x] Tests cover frontmatter, escaping, links, headings, and generated index output.
- [x] Blog post documents the harness trial.
