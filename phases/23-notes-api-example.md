# Phase 23: Notes API Example

## Goal

Use Kodr to generate the next example app: a small HTTP notes API.

## Scope

- [x] Pick the next-hardest example from the candidate list.
- [x] Add a prompt for the notes API example.
- [x] Attempt generation through Kodr and preserve artifacts.
- [x] Apply or complete the app under `examples/notes-api`.
- [x] Run the example app tests from its own directory.
- [x] Update Kodr if the trial exposes a harness gap.

## Done Criteria

- [x] `examples/notes-api` has a package, source, tests, and README.
- [x] The example supports JSON HTTP routes for create, list, read, update, and delete.
- [x] Tests cover routing, validation, persistence, and not-found behavior.
- [x] Blog post documents the harness trial.
