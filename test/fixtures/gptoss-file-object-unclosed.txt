{"status":"OK","messages":[{"level":"info","content":"Creating tasks CLI implementation and tests"}],"files":[{"path":"src/store.mjs","content":"import { readFile, writeFile } from 'node:fs/promises';\nimport { existsSync } from 'node:fs';\n\n/**\n * Load tasks from a JSON file.\n * @param {string} path - Path to the JSON file.\n * @returns {Promise<Array<{id:number,title:string,done:boolean}>>}\n */\nexport async function loadTasks(path) {\n  if (!existsSync(path)) return [];\n  const data = await readFile(path, 'utf8');\n  try {\n    return JSON.parse(data);\n  } catch {\n    // If file is corrupted, start fresh\n    return [];\n  }\n}\n\n/**\n * Save tasks to a JSON file.\n * @param {string} path - Path to the JSON file.\n * @param {Array<{id:number,title:string,done:boolean}>} tasks\n */\nexport async function saveTasks(path, tasks) {\n  const data = JSON.stringify(tasks, null, 2);\n  await writeFile(path, data, 'utf8');\n}\n"],"patches":[],"scratchpad":""}