{
  "name": "kodr-by-codex",
  "version": "0.0.33",
  "private": true,
  "type": "module",
  "bin": {
    "kodr": "./bin/kodr.mjs"
  },
  "scripts": {
    "test": "node --test",
    "check": "node --check bin/*.mjs && node --check src/*.mjs && node --check test/*.mjs && node --check test-support/*.mjs && find examples -name '*.mjs' -print0 | xargs -0 -n1 node --check && npm run cversion",
    "cversion": "node bin/cversion.mjs --check",
    "format": "biome format --write bin src test test-support examples",
    "install-local": "node bin/install-local.mjs"
  },
  "engines": {
    "node": ">=24"
  }
}
